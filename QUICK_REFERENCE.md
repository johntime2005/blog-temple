# Firefly 博客高级文章管理 - 快速参考

## 🎉 新功能总览

你的博客现在拥有了全面的文章管理系统！以下是所有可用的控制选项：

### ✅ 已实现的功能

1. **可见性控制** - 精细控制文章显示位置
2. **自定义排序** - 完全掌控文章顺序
3. **推荐级别** - 标记高质量内容
4. **布局模板** - 不同文章使用不同布局
5. **SEO 控制** - 精准控制搜索引擎行为
6. **隐私保护** - 多种隐私保护方案

---

## 📝 快速使用示例

### 示例 1：精选推荐文章（置顶显示）

```yaml
---
title: 必读教程
published: 2025-11-17
customOrder: 1          # 最高优先级
featuredLevel: 5        # 最高推荐级别
pinned: true
---
```

### 示例 2：低调分享的内部文档

```yaml
---
title: 内部项目文档
published: 2025-11-17
visibility: "unlisted"   # 仅链接访问
hideFromHome: true
hideFromSearch: true
seoNoIndex: true
---
```

### 示例 3：旧文章归档

```yaml
---
title: 2020年总结
published: 2020-12-31
hideFromHome: true      # 不在首页显示
showInWidget: false     # 不在侧边栏显示
customOrder: 9999       # 排到最后
---
```

### 示例 4：加密文章

```yaml
---
title: 私密日记
published: 2025-11-17
encrypted: true
password: "mysecret123"
---
```

**⚠️ 警告**：这**不安全**！
- 密码会被提交到 Git（任何人可见）
- 前端加密可被轻松绕过
- **仅适用于**：防搜索引擎、礼貌性保护
- **不适用于**：真正的隐私内容

**真正的安全方案**：使用 Git-Crypt 或 .gitignore

查看详细说明：`ENCRYPTION_SECURITY_WARNING.md`

---

## 🔑 关键字段说明

### 可见性控制

| 字段 | 类型 | 作用 |
|------|------|------|
| `visibility` | `"public" \| "unlisted" \| "private"` | 全局可见性 |
| `hideFromHome` | `boolean` | 从首页隐藏 |
| `hideFromArchive` | `boolean` | 从归档页隐藏 |
| `hideFromSearch` | `boolean` | 从搜索结果隐藏 |
| `showInWidget` | `boolean` | 是否在侧边栏显示 |

### 排序与推荐

| 字段 | 类型 | 作用 |
|------|------|------|
| `customOrder` | `number` | 自定义排序（数字越小越靠前） |
| `featuredLevel` | `0-5` | 推荐级别 |

### 布局与 SEO

| 字段 | 类型 | 作用 |
|------|------|------|
| `postLayout` | `"default" \| "wide" \| "fullscreen" \| "no-sidebar"` | 布局模板 |
| `seoNoIndex` | `boolean` | 禁止搜索引擎索引 |
| `seoNoFollow` | `boolean` | 禁止跟踪链接 |

---

## 🛡️ 隐私保护方案对比

### ⚠️ 重要警告

**所有提交到 Git 的文件都是可见的！** `draft` 和 `visibility` 只控制是否生成页面，**不能保护源文件**。

| 方案 | 隐私级别 | 文件在 Git | 使用场景 |
|------|---------|-----------|---------|
| **`.gitignore` 排除** | ⭐⭐⭐⭐⭐ | ❌ 不提交 | **绝对隐私（推荐）** |
| **GPG 加密后提交** | ⭐⭐⭐⭐⭐ | ✅ 加密 | 云端备份 + 隐私 |
| `draft: true` | ⭐⭐ | ✅ 明文 | 草稿，内容仍可见 |
| `visibility: "private"` | ⭐⭐ | ✅ 明文 | 同上 |
| `visibility: "unlisted"` | ⭐ | ✅ 明文 | 半公开 |
| `encrypted: true` | ⭐⭐⭐ | ✅ 明文 | 前端密码保护（**不安全！**） |

### 真正的隐私方案

**方法 1：使用 `.gitignore`（最简单）**

```bash
# 在 .gitignore 中添加
src/content/posts/private/

# 创建私密目录
mkdir -p src/content/posts/private
```

**方法 2：使用私有仓库**

将整个仓库设为私有，限制访问权限。

**方法 3：加密后提交**

```bash
gpg --encrypt --recipient your@email.com secret.md
git add secret.md.gpg
```

### ⚠️ 永远不要在公开仓库存储

- 🚫 密码、密钥、Token
- 🚫 个人隐私信息
- 🚫 公司机密

---

## 📚 完整文档

- **详细指南**: `ADVANCED_POST_MANAGEMENT.md`
- **项目文档**: `CLAUDE.md`
- **演示文章**: `src/content/posts/advanced-features-demo.md`

---

## 🚀 开始使用

只需在你的文章 frontmatter 中添加需要的字段：

```yaml
---
title: 我的文章
published: 2025-11-17
# 添加你需要的控制选项
hideFromHome: false
featuredLevel: 3
postLayout: "wide"
---
```

## 测试服务器

运行 `pnpm dev` 启动开发服务器，访问 http://localhost:4321 查看效果。

演示文章将显示在首页最前面（因为设置了 `customOrder: 1` 和 `featuredLevel: 5`）。

---

**享受更强大的文章管理体验！** 🎉
