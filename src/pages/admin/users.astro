---
import UserManagement from "@components/auth/UserManagement.svelte";
import Layout from "@layouts/Layout.astro";
---

<Layout
	title="用户管理后台"
	description="管理用户账户"
>
	<div class="admin-container">
		<div class="admin-content">
			<div class="header">
				<h1>用户管理</h1>
				<p>创建和管理博客用户账户</p>
				<a href="/admin/" class="back-link">← 返回管理后台</a>
			</div>

			<UserManagement client:load />

			<div class="instructions">
				<h3>📝 使用说明</h3>
				<ul>
					<li>需要先以站长身份<a href="/login/">登录</a>，本页会自动使用当前登录态</li>
					<li>用户名只能包含字母、数字和下划线，长度3-20个字符</li>
					<li>密码长度至少为6个字符</li>
					<li>可以使用"生成随机密码"功能创建安全的随机密码</li>
					<li>创建的用户可以登录访问受保护的内容（如日记）</li>
				</ul>

				<h3>🔐 权限说明</h3>
				<p>文章的 <code>accessLevel</code> 字段控制访问权限：</p>
				<ul>
					<li><code>public</code> - 所有人可访问（默认）</li>
					<li><code>members-only</code> - 仅登录用户可访问</li>
					<li><code>restricted</code> - 仅特定用户可访问（未来扩展）</li>
				</ul>
			</div>
		</div>
	</div>
</Layout>

<style>
	.admin-container {
		min-height: 100vh;
		background: var(--page-bg);
		padding: 2rem 1rem;
	}

	.admin-content {
		max-width: 1200px;
		margin: 0 auto;
	}

	.header {
		margin-bottom: 2rem;
		text-align: center;
	}

	.header h1 {
		font-size: 2rem;
		margin-bottom: 0.5rem;
		color: var(--text-color);
	}

	.header p {
		color: var(--text-secondary);
		font-size: 1rem;
	}

	.back-link {
		display: inline-block;
		margin-top: 0.75rem;
		color: var(--primary);
		text-decoration: none;
		font-size: 0.875rem;
	}

	.back-link:hover {
		text-decoration: underline;
	}

	.instructions {
		background: var(--card-bg);
		border-radius: 12px;
		padding: 2rem;
		margin-top: 2rem;
		box-shadow: var(--shadow-md);
	}

	.instructions h3 {
		margin-top: 0;
		margin-bottom: 1rem;
		color: var(--text-color);
		font-size: 1.125rem;
	}

	.instructions h3:not(:first-child) {
		margin-top: 2rem;
	}

	.instructions ul,
	.instructions ol {
		margin: 0.5rem 0;
		padding-left: 1.5rem;
	}

	.instructions li {
		margin: 0.5rem 0;
		color: var(--text-secondary);
		line-height: 1.6;
	}

	.instructions p {
		margin: 0.5rem 0;
		color: var(--text-secondary);
		line-height: 1.6;
	}

	.instructions code {
		background: var(--page-bg);
		padding: 0.125rem 0.375rem;
		border-radius: 4px;
		font-family: 'Courier New', monospace;
		font-size: 0.875rem;
		color: var(--primary);
	}

	.instructions a {
		color: var(--primary);
		text-decoration: none;
		transition: opacity 0.2s ease;
	}

	.instructions a:hover {
		opacity: 0.8;
		text-decoration: underline;
	}

	@media (max-width: 640px) {
		.admin-container {
			padding: 1rem 0.75rem;
		}

		.instructions {
			padding: 1.25rem;
		}
	}
</style>
